#include <stdio.h>

int main(void)
{
    int n = 0;

    // Check if (n >= 1 <= 10) is true
    if (n >= 1 <= 10)
        printf("n is between 1 and 10\n");

    // Return status code '0'
    return 0;
}
