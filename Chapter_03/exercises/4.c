#include <stdio.h>

int main(void)
{
    float x;
    int i, j;

    // Asks user for i, x and j
    scanf("%d%f%d", &i, &x, &j);
    printf("i: %d\n", i);
    printf("x: %f\n", x);
    printf("j: %d\n", j);

    // Return status code '0'
    return 0;
}
