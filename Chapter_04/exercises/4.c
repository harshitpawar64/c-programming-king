#include <stdio.h>

int main(void)
{
    // (a)
    printf("8 / 5: %d\n", 8 / 5); // 1

    // (b)
    printf("-8 / 5: %d\n", -8 / 5); // -1

    // (c)
    printf("8 / -5: %d\n", 8 / -5); // -1

    // (d)
    printf("-8 / -5: %d\n", -8 / -5); // 1

    // Return status code '0'
    return 0;
}
